# OnlyPeak frontend env. Copy to `.env` (gitignored) and adjust.
# Base URL of the personal FastAPI backend (see /backend).
# Leave empty to use the dev proxy ("/api" -> backend) configured in vite.config.js.
VITE_API_BASE_URL=
