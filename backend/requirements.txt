# OnlyPeak backend dependencies (Python 3.11+)
# Loose pins on purpose: this is a personal, single-user tool.

fastapi>=0.110
uvicorn[standard]>=0.29

# IMPORTANT: yt-dlp is a moving target. YouTube changes its internals
# frequently, which breaks extraction. Update this OFTEN:
#     pip install -U yt-dlp
# Do not pin it tightly; always run a recent version.
yt-dlp>=2024.1.1

# Async HTTP client used to proxy/stream the upstream googlevideo audio
# (required for HTTP Range support so <audio> can seek).
httpx>=0.27
